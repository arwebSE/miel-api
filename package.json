{
    "name": "miel-api",
    "version": "1.2.0",
    "description": "Serverless API for weather and geolocation on Vercel.",
    "main": "index.js",
    "scripts": {
      "dev": "vercel dev",
      "test": "NODE_ENV=test nyc --reporter=html mocha 'test/**/*.js' --exit --timeout 10000"
    },
    "dependencies": {
      "node-cache": "^5.1.2",
      "node-fetch": "^3.2.8"
    },
    "devDependencies": {
      "nyc": "^15.1.0",
      "mocha": "^9.1.3"
    },
    "engines": {
      "node": ">=14"
    }
  }
  